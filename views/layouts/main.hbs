<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/favicon.ico"/>
    <title>Javolin - Digital Wallet that works for you</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="stylesheet" type="text/css" href="../../styles/index.css" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script defer src="https://cdn.jsdelivr.net/npm/@alpinejs/intersect@3.x.x/dist/cdn.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    {{!-- <script defer src="https://unpkg.com/@alpinejs/intersect@3.x.x/dist/cdn.min.js"></script> --}}
    {{!-- <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script> --}}
    {{!-- <script src="https://unpkg.com/axios/dist/axios.min.js"></script> --}}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.3.4/axios.min.js"></script>
    <script src="/js/alphineData.js" ></script>
    
    <script src = "https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

<script type="text/javascript">
function googleTranslateElementInit() {
  new google.translate.TranslateElement({pageLanguage: 'en', includedLanguages: "en,fr", layout: google.translate.TranslateElement.InlineLayout.SIMPLE}, 'google_translate_element');
}
</script>

<script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

    <meta name="author" content="Javolin LLC | smith sese" />
    <meta name="description" content="Digital Wallet that works for you">
    <style>
        @font-face { font-family: 'Product-Sans'; src: url('/fonts/product-sans/font-regular.ttf'); }
        @font-face { font-family: 'Product-Sans'; font-weight: bold; src: url('/fonts/product-sans/font-bold.ttf');}
        @font-face { font-family: 'Satoshi'; src: url('/fonts/satoshi/Satoshi-Regular.ttf'); }
        @font-face { font-family: 'Satoshi'; font-weight: bold; src: url('/fonts/satoshi/Satoshi-Bold.ttf');}

        .goog-te-gadget-icon {
            display:none;
        }

        body > .skiptranslate > iframe.skiptranslate {
        display: none !important;
        visibility: hidden !important;
        }
        .goog-te-banner-frame.skiptranslate {
            display: none !important;
            } 
        

        .goog-te-banner-frame{
            display:none !important
        }

        .goog-te-banner-frame .VIpgJd-ZVi9od-ORHb{
            display:none !important
        }


    .goog-te-gadget-simple {
        background-color: #ecebf0 !important;
        border:0 !important;
        font-size: 10pt;
    font-weight:800;
        display: inline-block;
        padding:10px 10px !important;
        cursor: pointer;
        zoom: 1;
    }

    .goog-te-gadget-simple  span {
    color:#3e3065 !important;

    }
    .goog-te-gadget-simple .VIpgJd-ZVi9od-xl07Ob-lTBxed {
    color: #000;
    display: flex !important;
}
        body{
            font-family: 'Satoshi', sans-serif;
            margin: 0;
            top: 0px !important; 
            position: static !important; 
        }
        h2 {
            font-family: 'Satoshi';
            font-weight: bold;
            font-size: 54px;
        }
        .nav-link {
            font-family: 'Satoshi';
        }


        button {
            font-family: 'Satoshi';
        }

        ::marker{
            margin-top: 1em;
        }

        #imageContainerHero {
     
        position: relative;
        overflow: hidden;
        }

        #imageContainerHero img {
            position: absolute;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0;
      transition: transform 1s, opacity 1s;
        }
       @keyframes slideInFromBottom {
    0% {
        transform: translateY(100%); /* Starts below the visible area of #heroSec */
        opacity: 0;
    }
    100% {
        transform: translateY(0); /* End at the initial position */
        opacity: 1;
    }
}

#heroSec {
    position: relative;
    width: 100%;  /* Full width */
    height: 100vh; /* Full viewport height */
    display: flex;
    align-items: center;
    justify-content: center;
}

#textSection {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    text-align: center; /* Center text horizontally */
    display: flex;
    flex-direction: column;
    justify-content: center; /* Center content vertically within the text section */
}

.imageTextHero {
    width: 956px;
    margin: 0 auto; /* Centering horizontally if the text section is wider */
    text-align: center;
    font-size: 76px;
    font-family: 'Satoshi', sans-serif;
    font-weight: 900;
    color: white;
    animation: slideInFromBottom 1s ease-out forwards; /* Add the animation */
}

        /*}*/
    </style>
</head>

<body class="relative" x-data="{
                playerOpened: false,
                comingSoonOpened: false,
                waitListOpened: false,
                supportOpened:false,
                {{!-- language: {{language}}, --}}
                togglePlayer() {
                    if (this.open) {
                        return this.closePlayer()
                    }
                    $refs.javolinVideoHolder.classList.remove('hidden')
                    this.playerOpened = true
                },
                toggleWaitList() {
                    if (this.waitListOpened) {
                        this.waitListOpened = false
                        return
                    }
                    $refs.waitListHolder.classList.remove('hidden')
                    this.waitListOpened = true
                },
                toggleSupport() {
                    if (this.supportOpened) {
                        this.supportOpened = false
                        return
                    }
                    $refs.supportOpenedHolder.classList.remove('hidden')
                    this.supportOpened = true
                },
                toggleComingSoon() {
                    if (this.comingSoonOpened) {
                        this.comingSoonOpened = false
                        return;
                    }
                    $refs.comingSoonHolder.classList.remove('hidden')
                    this.comingSoonOpened = true
                },
                closePlayer(focusAfter) {
                    this.playerOpened = false
                    this.waitListOpened = false
                    var video = document.getElementById('mJavolinVideo');
                    video.pause();
                    video.currentTime = 0;
                    focusAfter && focusAfter.focus()
                },
                langs: {
                        'EN': {
                            lang_code: 'EN',
                            image: 'usflag.png'
                        },
                        'FR': {
                            lang_code: 'FR',
                            image: 'frflag.png',
                        },
                        {{!-- 'ES': {
                            lang_code: 'ES',
                            image: 'esflag.svg',
                        } --}}
                    },
                selectedLang: 'EN',
                dropdownOpened: false,
                langdata: [
                    { id: 'EN', url: 'usflag.png' },
                    { id: 'FR', url: 'frflag.png' },
                    {{!-- { id: 'ES', url: 'esflag.svg' }, --}}
                ],
                language:{
                    'EN':{
                        'Get a demo':'Get a demo',
                        'Industries': 'Industries',
                        'Solutions': 'Solutions',
                        'Technology': 'Technology',
                        'Resources': 'Resources',
                        'About us': 'About us',
                        'Support': 'Support',
                        
                    },
                    'FR':{
                        'Get a demo':'Connecter',
                        'Industries': 'Secteurs',
                        'Solutions': 'Solutions',
                        'Platform': 'Plateforme',
                        'Resources': 'Ressources',
                        'About us': 'Á notre propos',
                        'Support': 'Soutien',
                    },
                }
            }"
      x-id="['dropdown-button']">


<header
    class="font-medium flex justify-between items-center lg:px-12 xl:px-20 fixed z-30 rounded-full bg-white shadow-lg"
    :class="{'w-full top-0 rounded-none shadow-none': isAtTop, 'w-[1248px] top-12 text-black': !isAtTop}"
    x-data="{ isAtTop: false }" @scroll.window="isAtTop = window.scrollY <= 20"
    style="left: 50%; transform: translateX(-50%) translateZ(0); padding-left: 176px; transition: width 0.5s ease-out, top 0.5s ease-out, border-radius 0.5s ease-out, background-color 0.5s ease-out, box-shadow 0.5s ease-out, color 0.5s ease-out;"
    :style="{
        'height': isAtTop ? '70px' : '64px',
        'padding-top': isAtTop ? '0' : '8px',
        'padding-bottom': isAtTop ? '0' : '8px'
    }">

    <a href="/" class="absolute" style="top: 20px; left: 36px;">
        <img src="/images/jav_logo_color.png" alt="Javolin Logo" style="width: 130px; height: 30.23px;" />
    </a>

    <div class="flex-grow flex justify-center items-center gap-12">
        {{> nav }}
    </div>

    <a href="https://app.forex.javolin.com/" x-text="language[selectedLang]['Get a demo']"
        class="hidden lg:flex cursor-pointer bg-[#0032A0] text-white px-6 py-2 rounded-full text-lg font-bold items-center justify-center transition duration-300 ease-in-out"
        style="position: absolute; right: 20px; top: 50%; transform: translateY(-50%); white-space: nowrap;">
        Get a demo
    </a>
</header>









{{{ body }}}

{{!-- {{>download-app}} --}}

{{>footer}}

{{>pop-ups/javolin-video}}
{{>pop-ups/coming-soon}}
{{>pop-ups/wait-list}}
{{>pop-ups/support}}



<script>

        
    document.addEventListener('alpine:init', () => {
        
        Alpine.data('supportMessage', () => ({
            isLoading: false,
            user_email: '',
            name: '',
            message: '',
            subject:'',
            country:'',
            toggle() {
                this.isLoading = !this.isLoading
            },
            sendSupportMessage() {
                this.toggle();
                axios.post("https://staging.api.javolin.com/api/support",{
                    name: this.name,
                    email: this.user_email,
                    body: this.message,
                    subject: this.subject,
                    country: this.country,
                }).then(res => {
                    this.toggle();
                    console.log(res);
                    alert("Success: Message sent successfully");
                    this.name = "";
                    this.subject = "";
                    this.message = "";
                    this.user_email = "";
                    this.country = ""
                    
                }).catch(err => {
                    this.toggle();
                    console.error(err);
                    alert("Error: "+err.message)
                })
            },
        })),
        Alpine.data('waitList', () => ({
            isLoading: false,
            first_name: '',
            last_name : '',
            email : '',
            referral_code: '',
            phone_number : '',
            toggle() {
                this.isLoading = !this.isLoading
            },
            joinWaitList() {
                this.toggle();
                axios.post("https://staging.api.javolin.com/api/waitlist/create",{
                    first_name: this.first_name,
                    last_name: this.last_name,
                    email: this.email,
                    phone_number: this.phone_number,
                    referral_code: this.referral_code
                }).then(res => {
                    this.toggle();
                    console.log(res);
                    alert("Success: Joined successfully");
                    this.first_name = "";
                    this.last_name = "";
                    this.phone_number = "";
                    this.email = "";
                    this.referral_code = "";
                }).catch(err => {
                    this.toggle();
                    console.error(err);
                    alert("Error: "+err.message)
                })
            },
            
        }))
    })



     $(document).ready(function() {

        $('#google_translate_element').bind('DOMNodeInserted', function(event) {
      $('.goog-te-menu-value span:first').html('LANGUAGE');
      $('.goog-te-menu-frame.skiptranslate').load(function(){
        setTimeout(function(){
          $('.goog-te-menu-frame.skiptranslate').contents().find('.goog-te-menu2-item-selected .text').html('LANGUAGE');    
        }, 100);
      });
    });
      



    
       



        /** ---------------------------- //
         *  @group viewport trigger script
         * for adding or removing classes from elements in view within viewport
        */


        var $animationElements = $('.animation-element');
        var $window = $(window);

        // ps: Let's FIRST disable triggering on small devices!
        var isMobile = window.matchMedia("only screen and (max-width: 768px)");
        if (isMobile.matches) {
            $animationElements.removeClass('animation-element');
        }


        function checkIfInView() {

            var windowHeight = $window.height();
            var windowTopPosition = $window.scrollTop();
            var windowBottomPosition = (windowTopPosition + windowHeight);

            $.each($animationElements, function () {
                var $element = $(this);
                var elementHeight = $element.outerHeight();
                var elementTopPosition = $element.offset().top;
                var elementBottomPosition = (elementTopPosition + elementHeight);

            //check to see if this current container is within viewport
                        if ((elementBottomPosition >= windowTopPosition) &&
                            (elementTopPosition <= windowBottomPosition)) {
                            $element.addClass('in-view');
                        } else {
                            $element.removeClass('in-view');
                        }
                    });
                }

        $window.on('scroll resize', checkIfInView);
        $window.trigger('scroll');
    });
            
</script>




</body>



</html>